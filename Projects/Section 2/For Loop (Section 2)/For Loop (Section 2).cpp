#include <iostream>
#include <cmath>
#include <string>
#include <cassert>  // Include assert header

// Bring the standard namespace into scope
using namespace std;

// Main function entry point
int main(int argc, char **argv)
{
    // For loop to iterate 10 times
    for (int i=0; i<10; i++)
    {
        std::cout << i << " ";  // Print current value of i and space
    }
    
    return 0;  // Successful program execution
}
